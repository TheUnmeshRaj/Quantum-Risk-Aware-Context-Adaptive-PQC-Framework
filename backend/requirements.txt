# Backend requirements for UNISYS PQC Framework
# Minimal set inferred from backend/ imports (fastapi app, pydantic models,
# cryptography for demo crypto, qiskit for quantum simulations, gymnasium for envs).

fastapi==0.95
uvicorn[standard]==0.22
pydantic==2.0
cryptography==40.0
qiskit==0.40
qiskit-aer==0.12
gymnasium==0.28
numpy==1.24
typing-extensions==4.0

# Optional / recommended (performance):
# orjson>=3.9
